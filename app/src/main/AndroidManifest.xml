<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.phantomorion.tsechacks">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logohackathon"
        android:label="N-Jiyo"
        android:roundIcon="@drawable/logohackathon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".signInOptions" />
        <activity
            android:name=".NoInternet"
            android:label="No Internet Connection" />
        <activity android:name=".InformationForm" />
        <activity android:name=".UserMain" />
        <activity android:name=".BottomNavigationMainActivity"></activity>
        <activity android:name=".AddEventsAdminForm" />
        <activity android:name=".splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>